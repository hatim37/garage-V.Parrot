{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }}- Modifier une option
{% endblock %}

{% block body %}
	<div class="container body-height">

		<h1 class="mt-4">Modification d'une option</h1>
		<div class="row g-3">
			{{ form_start(form) }}

			<div class="form-group col-md-6">
				{{ form_label(form.name) }}
				{{ form_widget(form.name) }}
				<div class="form-error">
					{{ form_errors(form.name) }}
				</div>
			</div>

			<div class="width">
				<div class="form-group width d-flex justify-content-between">
					{{ form_label(form.submit) }}
					{{ form_widget(form.submit) }}
					<div class="d-flex align-items-end">
						<a href="{{ path('equipment.index') }}" class="btn btn-primary">Annuler</a>
					</div>
				</div>

			</div>

			{{ form_rest(form) }}
		</div>
	</div>
{% endblock %}

{% block javascripts %}
	{{ parent() }}
	<script src="{{asset('assets/js/select2.js')}}"></script>
{% endblock %}
